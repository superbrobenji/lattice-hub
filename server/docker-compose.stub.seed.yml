# Seed overlay — mounts stub-data/ into the orchestrator's data directory.
# Apply on top of docker-compose.stub.yml: make stub-seed

services:
  orchestrator:
    volumes: !reset
      - ./stub-data:/app/data
